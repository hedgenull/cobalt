# Compute the Fibonacci Sequence

Module {
    Var a = 1;
    Var b = 1;

    PrintLn("Enter a number: ");
    InputNum i;

    While i > 0 {
        Print(a + b);
        Var c = a;
        Var a = b;
        Var b = b + c;

        Var i = i - 1;
    };
};